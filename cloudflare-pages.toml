# Cloudflare Pages configuration for RepeatingImageTransition SPA

project_name = "repeating-image-transition"
account_id = "<your-account-id>"

[build]
command = "npm run build"
directory = "dist"

[dev]
command = "npm run dev"
port = 3000
